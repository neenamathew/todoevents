<h1>Editing Todoevent</h1>

<%= form_for(@todoevent), remote: true do |f| %>
  <% if @todoevent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todoevent.errors.count, "error") %> prohibited this todoevent from being saved:</h2>

      <ul>
      <% @todoevent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" id= "id_of_your_textbox">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
 
<% end %>


<%= link_to 'Show', @todoevent %> |
<%= link_to 'Back', todoevents_path %>
